<!DOCTYPE html>
<html>
<head>
    <title>Product Card</title>
<style>
.card {
  box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2);
  max-width: 300px;
  margin: auto;
  text-align: center;
  font-family: arial;
}

.price {
  color: grey;
  font-size: 22px;
}

.card button {
  border: none;
  outline: 0;
  padding: 12px;
  color: white;
  background-color: #000;
  text-align: center;
  cursor: pointer;
  width: 100%;
  font-size: 18px;
}

.card button:hover {
  opacity: 0.7;
}
</style>
</head>
<body>

<h2 style="text-align:center">Product Card</h2>

    {{#each data}}
    <div class="card">
    <img src="{{this.products_image}}" alt="" style="width:100%">
    
    <h1>ID: {{this.products_id}}</h1>
    <p class="price">${{this.final_price}}</p>
    <p>Model: {{this.products_model}}</p>

    </div>
    {{/each}}
</body>
</html>

